name: aplikasi_absensi
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  dio: ^5.9.1
  flutter:
    sdk: flutter
  http: ^1.6.0
  mobile_scanner: ^7.1.4
  open_filex: ^4.7.0
  path_provider: ^2.1.5
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/gambar1.png
    - assets/LOGO.png
